
git clone NaSiK [dev-kvm]

git submodule init
git submodule update
 -- Hard-coded submodule !!!

git clone linux-kvm [dev]

modify the hardlink in 
-- kroger : fdaccess.0.0 !!!
-- hw : for systemc !!!

git clone Apes.git [dev]

build apes-i386 toolchain

install systemc-2.2.0
-- attention à dest repo !!!

compile application MJPEG
-- Hardcoded path for KVMx86SecondaryBoot.o
-- Format KVM Boot loader for using APES infrstructure

compilation of LLVM + LLVM GCC 
-- hack for C6x Decoder ... (Include Absolute Paths in Makefile.common in C6xDecoder Directory)
# Set this variable to the top of the LLVM source tree.
LLVM_SRC_ROOT = /home/hamayun/workspace/NaSiK/sw/llvm
LLVM_OBJ_ROOT = /home/hamayun/workspace/Build/llvm-2.8/

Sources for LLVM-GCC ... ?? 
Compile from sources [Otherwise some strange problems appear]


